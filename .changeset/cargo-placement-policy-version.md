---
'@adatechnology/cargo-placement': minor
---

Exporta `CARGO_LAYOUT_POLICY_VERSION`, a versão do desenho que entra no `input_hash` de toda planta
guardada por quem consome o pacote. Sobe só quando a mesma entrada passa a produzir outro desenho.
