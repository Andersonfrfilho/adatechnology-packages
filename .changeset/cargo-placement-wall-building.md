---
'@adatechnology/cargo-placement': minor
---

Baú fechado monta em parede (fileira a partir da cabeceira, cada pilha subindo até o teto), a vizinha escora
com 80% da borda, e uma passada final coloca por cima de entrega anterior o que sobrou, marcado com
`overEarlierDelivery` e `coversStops`. As caixas de fora vêm por nota, com `documentId`. As passadas finais
respeitam o prazo. `CARGO_LAYOUT_POLICY_VERSION` sobe para `'6'`.
