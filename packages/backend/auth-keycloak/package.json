{
  "name": "@adatechnology/auth-keycloak",
  "version": "0.0.1",
  "description": "Keycloak JWT verification and middleware for Bun, uWebSockets.js, and Node.js",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "check": "tsc --noEmit"
  },
  "peerDependencies": {
    "@adatechnology/logger": "workspace:*"
  },
  "devDependencies": {
    "@adatechnology/logger": "workspace:*",
    "@types/bun": "latest",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": ["keycloak", "auth", "jwt", "bun", "uwebsockets"],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
